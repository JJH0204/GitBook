# Samba

I can convert that page into GitBook-optimized Markdown (using tabs, steppers, expandables, hints, code-block titles, etc.) and keep all links/URLs unchanged. I don't have the ability to fetch web pages directly from that URL right now — please do one of the following so I can proceed:

Options:

1. Paste the page HTML or Markdown content here (preferred). I will transform it into GitBook-ready Markdown following the rules you gave.
2. Paste the page plain text. I will extract structure and convert it.
3. Give me permission/credentials for a direct fetch (only if you can provide them and accept the security implications). Note: I cannot browse automatically without an explicit fetch mechanism.

What I'll produce when you provide the content:

* Preserve original structure and meaning; remove navigation headers/footers.
* Convert any long numbered procedure into a stepper block (removing numeric prefixes from step titles).
* Convert package-manager code blocks (npm, yarn, pnpm) into a tabs block (one tab per manager).
* Convert FAQ sections into expandable blocks.
* Use code blocks with titles where appropriate.
* Keep all links/URLs and query parameters unchanged.
* Do not add any new information.

If you paste the content, please also tell me:

* Do you want images kept as-is (inline), replaced by placeholders, or removed?
* Any specific filename/title for the resulting GitBook page?

Paste the content and I'll return the converted Markdown ready to paste into GitBook.
