# 리눅스 마스터 1차 기출 오답 노트

{% stepper %}
{% step %}
### 문제

다음 명령어에 대한 설명으로 가장 적절한 것은

`[root&ihd root]# cat /etc/passwd | grep -v linuxmaster`

옵션:

* 가. /etc/passwd 파일에서 linuxmaster라는 문자열이 포함된 행만 출력한다.
* 나. /etc/passwd 파일에서 linuxmaster라는 문자열이 포함되지 않은 행만 출력한다.
* 다. /etc/passwd 파일에서 linuxmaster라는 문자열을 추가한다.
* 라. /etc/passwd 파일에서 linuxmaster라는 문자열이 계정이 존재하는지 확인한다.

정답: 나. /etc/passwd 파일에서 linuxmaster라는 문자열이 포함되지 않은 행만 출력한다.

해설: grep 의 옵션 중 `-v`는 전달 받은 키 값을 포함하지 않는 문장(행)을 출력하는 옵션이다.
{% endstep %}

{% step %}
### 문제

현재 디렉토리의 하위 디렉토리까지 모두 포함하여 linuxmaster라는 문자열을 포함한 파일을 검색하는 명령으로 가장 알맞은 것은?

옵션:

* 가. find .-string linuxmaster -print
* 나. grep -r linuxmaster \*
* 다. ls-al | grep linuxmaster
* 라. cat \* | grep linuxmaster

정답: 나. grep -r linuxmaster \*

해설: `grep -r '찾는 키' '디렉토리'` 방식으로 디렉토리와 하위 디렉토리의 모든 파일을 검색한다. `*` 또는 `.`는 현재 디렉토리를 의미한다.
{% endstep %}

{% step %}
### 문제

shutdown 명령어의 옵션에 대한 설명으로 틀린 것은?

옵션:

* 가. -c: 예약된 shutdown 명령을 취소한다.
* 나. -h: shutdown 명령이 완료되면 시스템을 정지시킨다.
* 다. -f: shutdown 전에 수행중인 모든 프로세스에게 kill 시그널을 보낸다.
* 라. -r: shutdown 명령이 완료되면 시스템을 재부팅 한다.

정답: 다. -f: shutdown 전에 수행중인 모든 프로세스에게 kill 시그널을 보낸다.

해설: `-f` 옵션은 다음 부팅 시 파일 시스템 검사를 건너뛰도록 설정하는 옵션이다. 강제 종료 옵션이 아니다.
{% endstep %}

{% step %}
### 문제

diff 명령어의 옵션에 대한 설명으로 틀린 것은?

옵션:

* 가. -i: 대,소문자를 구별한다.
* 나. -b: 하나 이상의 공백문자는 같은 것으로 취급하여 비교한다.
* 다. -e: ed 에디터를 위한 스크립트를 생성한다.
* 라. -w: 공백을 무시하고 비교 작업을 수행한다.

정답: 가. -i: 대,소문자를 구별한다.

해설: `-i` 옵션은 대소문자를 구별하지 않고 파일을 비교한다.
{% endstep %}

{% step %}
### 문제

시스템을 재 시작하지 않고 종료하는 명령으로 틀린 것은?

옵션:

* 가. shutdown -h now
* 나. halt
* 다. init 0
* 라. reboot

정답: 라. reboot

해설: `reboot` 명령어는 시스템을 재부팅 하는 명령어다.
{% endstep %}

{% step %}
### 문제

FSF의 설립자로서 GNU를 이끌면서 리눅스의 발전에 핵심적인 역할을 한 사람은?

옵션:

* 가. 리처드 스톨만(Richard Stallman)
* 나. 리누스 토발즈(Linus Tovalds)
* 다. 앤드류 타넨바움(Andrew Tanenbaum)
* 라. 빌 게이츠(Bill Gates)

정답: 가. 리처드 스톨만(Richard Stallman)

해설: 리처드 스톨만은 1983년 FSF/GNU 프로젝트를 시작했다.
{% endstep %}

{% step %}
### 문제

시디롬을 열고 닫을 때, 사용하는 명령어는?

옵션:

* 가. eject
* 나. mount
* 다. mmd
* 라. close

정답: 가. eject

해설: `eject`는 CD-ROM 트레이를 열거나 닫는 데 사용된다. `mount`는 장치를 마운트한다.
{% endstep %}

{% step %}
### 문제

리차드 스톨만(Richard Stallman)에 의해 설립되었으며 컴퓨터 프로그램의 복제와 배포, 개작을 위한 소스 코드의 응용에 대한 제한들을 철폐하는 목적을 가진 단체는?

옵션:

* 가. FSF
* 나. ISO
* 다. ANSI
* 라. IETF

정답: 가. FSF

해설: FSF는 자유 소프트웨어 원칙을 추구한다.
{% endstep %}

{% step %}
### 문제

LILO에 대한 설명으로 틀린 것은?

옵션:

* 가. 반드시 MBR(Master Boot Record)에 설치되어야 하는 것은 아니다.
* 나. Redhat 계열의 배포판에서만 제공된다.
* 다. LILO를 사용하면 다양한 OS를 선택하여 사용할 수 있다.
* 라. LILO외에도 GRUB(Grand Unified Bootloader)등의 부트 로더가 있다.

정답: 나. Redhat 계열의 배포판에서만 제공된다.

해설: LILO는 특정 배포판에만 제공되는 것이 아니다.
{% endstep %}

{% step %}
### 문제

LILO 설정 파일인 /etc/lilo.conf의 각 설정에 대한 설명으로 틀린 것은?

옵션:

* 가. boot=/dev/hda : LILO가 설치될 위치
* 나. map=/boot/map : LILO에 의해서 자동으로 생성되는 파일
* 다. install=/boot/boot.b : 부트 섹터 위치 정보를 가진 파일
* 라. timeout=50 : 키보드 입력이 없을 시 자동 부팅시간 50초 설정

정답: 가. boot-/dev/hda : LILO가 설치될 위치

해설: `boot=`는 부트 로더가 시스템을 시작할 때 사용할 장치를 지정한다. `/dev/hda`는 첫 번째 IDE 하드 드라이브를 의미한다.
{% endstep %}

{% step %}
### 문제

오류 메시지를 파일로 저장하기 위한 방향 재지정 명령으로 옳은 것은?

옵션:

* 가. cat nofile 0> error\_log\_file
* 나. cat nofile 1> error\_log\_file
* 다. cat nofile 2> error\_log\_file
* 라. cat nofile > error\_log\_file

정답: 다. cat nofile 2> error\_log\_file

해설: `2>`는 표준 에러 스트림을 리디렉션하는 데 사용된다.
{% endstep %}

{% step %}
### 문제

LILO와 GRUB에 대한 설명으로 틀린 것은?

옵션:

* 가. LILO는 GRUB보다 먼저 개발되었다.
* 나. LILO는 컴퓨터 바이오스(BIOS)의 정보를 참조하지 않는다.
* 다. GRUB은 IDE 하드디스크를 장착한 순서대로 인식한다.
* 라. GRUB에서는 부트 디스크를 통한 부팅을 지원하지 않는다.

정답: 나. LILO는 컴퓨터 바이오스(BIOS)의 정보를 참조하지 않는다.

해설: LILO는 부트 로더로서 BIOS 정보를 참조한다.
{% endstep %}

{% step %}
### 문제

하나의 디스크를 몇 개의 드라이브로 분할하여 사용할지 설정하는 것으로 그 용어와 툴(Tool)의 조합이 맞는 것은?

옵션:

* 가. MBR,FDISK
* 나. MBR,LILO
* 다. 파티션,Disk Druid
* 라. 파티션,LILO

정답: 다. 파티션,Disk Druid

해설: Disk Druid는 GUI 기반 디스크 파티셔닝 도구다.
{% endstep %}

{% step %}
### 문제

다음 중 논리 파티션에 부여될 수 있는 최소 파티션 값으로 알맞은 것은?

옵션:

*
  1. 1
*
  2. 2
*
  3. 4
*
  4. 5

정답: 5

해설: 논리 파티션은 보통 5번부터 시작한다.
{% endstep %}

{% step %}
### 문제

크론 데몬(cron daemon)에 의해 갱신된 데이터베이스를 바탕으로 파일의 빠른 검색을 가능하게 하는 명령어는?

옵션:

*
  1. locate
*
  2. grep
*
  3. find
*
  4. search

정답: locate

해설: `locate`는 `updatedb`로 갱신된 데이터베이스를 사용해 빠르게 검색한다.
{% endstep %}

{% step %}
### 문제

grub.conf 파일의 설정 내용이 지워진 상태에서 GRUB 부트 메뉴에서 관련 설정을 직접 입력하려고 한다. 다음 중 GRUB 부트 메뉴에서 입력하는 키로 알맞은 것은?

옵션:

*
  1. \[a]
*
  2. \[b]
*
  3. \[c]
*
  4. \[e]

정답: \[e]

해설: GRUB 부트 메뉴에서 항목을 편집하려면 'e'키를 눌러 편집 모드로 들어간다.
{% endstep %}

{% step %}
### 문제

소스 코드를 수정해서 프로그램을 개발한 후에 소스 코드를 공개하지 않고 독점적 소프트웨어로 사용하려고 한다. 다음 설명에 적합한 라이선스로 알맞은 것은?

옵션:

*
  1. GPL
*
  2. BSD
*
  3. MPL
*
  4. LGPL

정답: BSD

해설: BSD 라이선스는 소스 코드 공개 없이 수정 코드를 독점적으로 사용할 수 있도록 허용한다.
{% endstep %}

{% step %}
### 문제

`ls -l a? | wc -l` 명령어의 결과값은?

옵션:

*
  1. 2
*
  2. 3
*
  3. 4
*
  4. 5

정답: 2

해설: `a?`는 a로 시작하고 뒤에 한 글자가 있는 파일을 의미하며, 출력 라인 수를 센다.
{% endstep %}

{% step %}
### 문제

다음 명령의 실행결과에 대한 설명으로 가장 적절한 것은?

명령: `ls -al /etc/skel`

결과(생략)

옵션:

*
  1. 현재 로그인 된 계정의 bash 설정 파일들을 나열하고 있다.
*
  2. 새로운 계정을 생성할 때 기본적으로 홈 디렉토리에 생성되어야 할 각종 설정 파일들의 목록을 나열하고 있다.
*
  3. 현재 로그인된 계정의 홈 디렉토리에 존재하는 파일들을 나열하고 있다.
*
  4. 시스템의 모든 사용자에게 영향을 주는 bash 설정을 담당하는 파일들을 나열하고 있다.

정답: 2

해설: `/etc/skel`은 새 계정 생성 시 기본 홈 디렉토리에 복사되는 설정 파일들을 포함한다.
{% endstep %}

{% step %}
### 문제

다음 (괄호) 안에 들어갈 명령어로 알맞은 것은?

출력 예시: kaituser pts/0 2017-01-23 22:27 (203.247.40.xxx) root pts/2 2016-08-29 16:13 (:2.0) root tty1 2016-11-28 15:46 (:0) root pts/16 2016-11-29 09:13 (:0.0)

옵션:

*
  1. who
*
  2. w
*
  3. id
*
  4. users

정답: who

해설: `who`는 시스템에 로그인한 사용자들의 정보를 보여준다.
{% endstep %}

{% step %}
### 문제

다음 (괄호) 안에 들어갈 명령어로 알맞은 것은?

`# ( ) -s time.bora.net`

옵션:

*
  1. set
*
  2. time
*
  3. date
*
  4. rdate

정답: rdate

해설: `rdate`는 시간 서버에서 시간을 가져와 시스템 시간을 설정할 수 있다.
{% endstep %}

{% step %}
### 문제

다음 중 파티션 분할된 상태를 확인할 수 있는 파일로 알맞은 것은?

옵션:

*
  1. /etc/partition
*
  2. /etc
*
  3. /proc/partition
*
  4. /proc/partitions

정답: /proc/partitions

해설: `/proc/partitions`에는 시스템이 인식한 블록 장치와 파티션 정보가 포함되어 있다.
{% endstep %}

{% step %}
### 문제

다음 중 시스템에 로그인한 전체 사용자에게 메시지를 전달할 때 사용하는 명령으로 알맞은 것은?

옵션:

*
  1. write
*
  2. mesg
*
  3. message
*
  4. wall

정답: wall

해설: `wall`은 시스템에 로그인한 모든 사용자에게 메시지를 보낸다.
{% endstep %}

{% step %}
### 문제

다음 중 아파치 라이선스를 적용하는 소프트웨어로 알맞은 것은?

옵션:

*
  1. CentOS
*
  2. Hadoop
*
  3. Firefox
*
  4. OS XNIS

정답: Hadoop

해설: Hadoop은 Apache Software Foundation에서 개발되며 Apache License를 따른다.
{% endstep %}

{% step %}
### 문제

다음 (괄호) 안에 들어갈 내용으로 알맞은 것은?

`# date ( )` 출력: 2017 02 01

옵션:

*
  1. "%Y %m %d"
*
  2. -%Y %m %d
*
  3. +"%Y %m %d"
*
  4. \--%Y %m %d

정답: +"%Y %m %d"

해설: `date` 명령어에서 형식을 지정할 때 `+` 기호를 사용한다.
{% endstep %}

{% step %}
### 문제

다음 중 상용판 리눅스로 알맞은 것은?

옵션:

*
  1. CentOS
*
  2. Scientific Linux
*
  3. RHEL
*
  4. Fedora

정답: RHEL

해설: RHEL은 Red Hat에서 제공하는 상용(유료 지원) 리눅스 배포판이다.
{% endstep %}

{% step %}
### 문제

다음 중 리눅스에서 인식되는 장치 파일명의 종류가 나머지 셋과 다른 것은?

옵션:

*
  1. SCSI
*
  2. E-IDE
*
  3. SSD
*
  4. USB 메모리

정답: E-IDE

해설: E-IDE 장치는 `/dev/hd*`로 인식되고, 나머지는 `/dev/sd*`로 인식된다.
{% endstep %}

{% step %}
### 문제

다음 (괄호) 안에 들어갈 옵션으로 알맞은 것은?

예시: `[kaituser@www ~]$ cat ( ) lin.txt`

파일 내용(생략)

옵션:

*
  1. -b
*
  2. -n
*
  3. -v
*
  4. -s

정답: -n

해설: `-n`은 모든 줄에 번호를 매긴다.
{% endstep %}

{% step %}
### 문제

다음 파일에 대한 설명으로 틀린 것은?

옵션:

* 가. /etc/shadow는 일반 사용자는 읽기 권한만 가지며, 쓰기 및 실행은 할 수 없도록 지정되어있다.
* 나. /etc/shadow 파일에서 패스워드는 x로 표시되어 /etc/shadow 파일의 포인터를 유지하고 있다.
* 다. /etc/shadow 파일의 expire 필드는 암호와 계정이 만료되는 날짜의 정보를 가지고 있다.
* 라. /etc/passwd 파일은 사용자 계정에 대해 uid, gid, 기본 셸 등의 정보를 포함하고 있다.

해설: 가 항목이 틀리다. `/etc/shadow`는 일반 사용자에게 읽기 권한조차 주어지지 않으며, 보통 root만 읽기/쓰기 권한을 가진다.
{% endstep %}

{% step %}
### 문제

LILO 설정 파일인 /etc/lilo.conf의 각 설정에 대한 설명으로 틀린 것은?

옵션:

* 가. BOOT=/dev/gda : LILO가 설치 될 위치
* 나. MAP=/boot/map : LILO에 의해서 자동으로 생성되는 파일
* 다. INSTALL=/boot/boot.b 부트 섹터 위치 정보를 가진 파일
* 라. TIMEOUT=50 : 키보드 입력이 없을 시 자동 부팅시간 50초 설정

해설: 가 항목이 틀리다. `/dev/gda`는 잘못된 디바이스 이름이며 일반적으로 `/dev/sda`나 `/dev/hda` 등이 사용된다.
{% endstep %}

{% step %}
### 문제

리눅스 설치 시 사용자 보안 인증에 관한 설정사항이 아닌 것은?

옵션:

* 가. MD5
* 나. Shadow Password
* 다. NIS
* 라. SSL

정답: 라. SSL

해설: SSL은 네트워크 통신 암호화 프로토콜로, 리눅스 설치 시 사용자 보안 인증과 직접적인 관련은 없다.
{% endstep %}

{% step %}
### 문제

bash 기준으로 명령어의 검색 경로를 지정할 수 있는 파일은?

옵션:

* 가. bash\_scan
* 나. bash\_find
* 다. bash\_path
* 라. bash\_profile

정답: bash\_profile

해설: `bash_profile`에서 PATH 등의 환경변수를 설정할 수 있다.
{% endstep %}

{% step %}
### 문제

netstat 명령어에서 라우팅 테이블을 출력하는 옵션은?

옵션:

*
  1. -a
*
  2. -c
*
  3. -i
*
  4. -r

정답: -r

해설: `-r`은 라우팅 테이블을 보여주는 옵션이다.
{% endstep %}

{% step %}
### 문제

다음 명령의 의미는?

`source=\`ls \*.c\`\`

옵션:

*
  1. .c 확장자를 가진 파일을 찾아 화면에 나열한다.
*
  2. source 셸 변수에 'ls \*.c'라는 문자열을 대입한다.
*
  3. .c 확장자를 가진 파일을 찾아 source 셸 변수에 대입한다.
*
  4. source 변수의 내용과 ls \*.c 명령의 수행 결과가 동일한가 비교한다.

정답: 3

해설: 백틱 내부 명령의 실행 결과가 변수에 대입된다. \{% endstep %\}

\{% step %\}

### 문제

어떤 사람이 리눅스에서 인터넷을 사용하기 위해 브라우저를 통해 원격시스템과의 연결을 요청했다. 그런데 연결은 되지 않고 "unknown host" 오류 메시지만 나온다. 제일 먼저 점검해 보아야 할 사항은? (복수 가능, 주요 포인트는 2번과 4번)

옵션:

*
  1. /etc/host.conf 파일을 점검한다.
*
  2. hostname 명령을 수행한다.
*
  3. ifconfig 명령을 수행한다.
*
  4. /etc/resolv.conf 파일을 점검한다.

해설: `hostname`으로 호스트 이름 설정을 확인하고 `/etc/resolv.conf`에서 DNS 서버 설정을 확인한다. \{% endstep %\}

\{% step %\}

### 문제

인터넷 서비스 설명 중 옳지 않은 것은?

옵션:

*
  1. TELNET : 원격 시스템 접속 단말기 서비스
*
  2. FTP : 파일 송수신 서비스
*
  3. SMTP : 연결할 호스트의 이름에 대한 IP 주소를 찾아 주는 프로토콜
*
  4. HTTP : 웹을 통한 전자문서 서비스

정답: 3

해설: 호스트 이름에 대한 IP 주소를 찾아 주는 프로토콜은 DNS이다. SMTP는 메일 전송 프로토콜이다. \{% endstep %\}

\{% step %\}

### 문제

다음 명령의 실행 결과에 대한 설명으로 가장 적절한 것은?

명령: `[root@ihdroot]echo$PWD`\
출력: `/root`

옵션:

*
  1. 현재 작업 디렉토리의 위치를 확인해 보고 있다.
*
  2. 홈 디렉토리 정보를 간직하고 있는 PWD 환경변수의 내용을 출력하고 있다.
*
  3. 시스템 관리자의 권한으로만 위 환경변수를 확인해 볼 수 있다.
*
  4. 일반적으로 디렉토리를 이동하면 PWD 변수를 수동으로 다시 설정해 주어야 한다.

정답: 1 또는 2 설명의 요지 동일 — PWD는 현재 작업 디렉토리의 절대 경로를 저장한다.

해설: PWD는 현재 작업 디렉토리의 절대 경로를 저장하며 사용자가 디렉토리를 이동하면 자동으로 갱신된다. \{% endstep %\}

\{% step %\}

### 문제

다음은 mbox 파일이 어떤 종류의 파일인지 확인해 보기 위한 명령이다. () 안에 들어갈 가장 적절한 명령은?

`[root@ihdroot]# () mbox`\
출력: `mbox: ASCII mail text`

옵션:

*
  1. what
*
  2. file
*
  3. type
*
  4. tail

정답: file

해설: `file` 명령은 파일의 타입을 확인한다. \{% endstep %\}

\{% step %\}

### 문제

다음 중 리눅스에서 파티션을 분할하도록 권장하는 영역으로 틀린 것은?

옵션:

*
  1. /usr
*
  2. /tmp
*
  3. /etc
*
  4. /var

정답: /etc (틀린 항목)

해설: `/etc`는 정적 설정 파일을 저장하므로 별도 파티션으로 분리하는 것은 일반적 권장 대상이 아니다. \{% endstep %\}

\{% step %\}

### 문제

reboot 명령어의 수행 과정에 대한 설명으로 틀린 것은?

옵션:

*
  1. 파일 시스템을 언마운트한다.
*
  2. 시스템을 shutdown 한다.
*
  3. 시스템 실행 수준(run level)을 3으로 변경시킨다.
*
  4. 다중 사용자(multi-user) 모드에서는 수행하지 않는다.

해설: 3번과 4번은 부정확하다. reboot은 재부팅을 위해 정상 종료 절차를 수행하며, 다중 사용자 모드에서도 수행될 수 있다. \{% endstep %\}

\{% step %\}

### 문제

리처드 스톨먼(Richard Stallman)에 의해 설립된 단체는?

옵션:

*
  1. FSF
*
  2. ISO
*
  3. ANSI
*
  4. IETF

정해: FSF

해설: FSF는 자유 소프트웨어 원칙을 실천하는 단체이다. \{% endstep %\}

\{% step %\}

### 문제

E-IDE 타입의 디스크인 Secondary Slave에 연결될 경우 인식되는 장치 파일명은?

옵션:

*
  1. /dev/hda
*
  2. /dev/hdb
*
  3. /dev/hdc
*
  4. /dev/hdd

정답: /dev/hdd

해설: Secondary(두 번째 IDE 인터페이스) Slave(슬레이브) → /dev/hdd. \{% endstep %\}

\{% step %\}

### 문제

명령줄 명령해석에서 입력을 전환할 때 사용하는 기호로 알맞은 것은?

옵션:

*
  1. |
*
  2. >
*
  3. <
*
  4. ;

정답: |

해설: `|`(파이프)는 앞 명령의 출력을 다음 명령의 입력으로 전달한다.
{% endstep %}

{% step %}
### 문제

다음 중 우분투(Ubuntu) 리눅스의 기반이 되는 리눅스 배포판은?

옵션:

*
  1. Fedora
*
  2. RHEL
*
  3. Debian
*
  4. Slackware

정답: Debian

해설: 우분투는 Debian에서 파생되었다.
{% endstep %}

{% step %}
### 문제

다음 중 슬랙웨어 계열 리눅스에 속하는 배포판으로 알맞은 것은?

옵션:

*
  1. Kali Linux
*
  2. Scientific Linux
*
  3. SUSE
*
  4. Knoppix

정답: 주어진 선택지 중에는 슬랙웨어 계열이 없다.

해설: Kali, Scientific, Knoppix 등은 각각 Debian 또는 RHEL 계열이다. 슬랙웨어 계열 예: Slackware, Zenwalk, Salix 등.
{% endstep %}
{% endstepper %}
